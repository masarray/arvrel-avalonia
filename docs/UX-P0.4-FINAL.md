# P0.4 final design intent

The primary operator composition is the secondary-injection test set on the left and the virtual relay faceplate on the right. Supporting details remain available elsewhere and are not repeated in every injection row.
